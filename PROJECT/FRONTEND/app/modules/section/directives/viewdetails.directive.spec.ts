import { ViewdetailsDirective } from './viewdetails.directive';

describe('ViewdetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewdetailsDirective();
    expect(directive).toBeTruthy();
  });
});
